const tabs=[['deck','Command Deck'],['agents','Agents'],['tasks','Task Board'],['log','AI Log'],['council','Council'],['meetings','Meetings']]
export function TabBar({activeTab,onTabChange}:{activeTab:string;onTabChange:(tab:string)=>void}){return <nav aria-label="Dashboard sections" className="mb-6 rounded-lg bg-white/5 p-1"><div className="flex flex-wrap gap-1">{tabs.map(([id,label])=><button key={id} type="button" onClick={()=>onTabChange(id)} aria-current={activeTab===id?'page':undefined} className={`min-h-10 rounded-md px-3 py-2 text-sm outline-none focus-visible:ring-2 focus-visible:ring-emerald-400 ${activeTab===id?'bg-white/10':'text-dvn-text-secondary'}`}>{label}</button>)}</div></nav>}

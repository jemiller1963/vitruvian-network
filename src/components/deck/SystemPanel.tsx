import type{SystemSummary}from'@shared/contracts'
export function SystemPanel({system}:{system:SystemSummary}){const used=system.memory.totalBytes-system.memory.freeBytes;return <section className="glass-card p-5"><h2 className="mb-4 font-semibold">Local System</h2><div className="grid gap-4 sm:grid-cols-3"><div className="rounded-lg bg-white/5 p-3"><p className="text-xs text-dvn-text-secondary">Gateway</p><p className="mt-2 capitalize">{system.gateway.status}</p></div><div className="rounded-lg bg-white/5 p-3"><p className="text-xs text-dvn-text-secondary">Load average</p><p className="mt-2 font-mono">{system.loadAverage[0]?.toFixed(2)??'Unavailable'}</p></div><div className="rounded-lg bg-white/5 p-3"><p className="text-xs text-dvn-text-secondary">Memory used</p><p className="mt-2 font-mono">{system.memory.totalBytes?`${(used/1e9).toFixed(1)} GB / ${(system.memory.totalBytes/1e9).toFixed(1)} GB`:'Fixture unavailable'}</p></div></div>{system.gateway.reason&&<p className="mt-3 text-xs text-dvn-text-secondary">{system.gateway.reason}</p>}</section>}

import{ResponsiveContainer,LineChart,Line,XAxis,YAxis,Tooltip,Legend}from'recharts';import type{ActivityMetrics}from'@shared/contracts'
export function ActivityChart({activity}:{activity:ActivityMetrics}){const data=activity.daily.map(x=>({...x,label:new Date(x.start).toLocaleDateString(undefined,{weekday:'short'})}));return <section className="glass-card p-5"><div className="mb-4 flex justify-between"><h2 className="font-semibold">Seven-Day Activity</h2><span className="text-xs text-dvn-text-secondary">Timezone: {activity.timezone}</span></div><div className="h-64"><ResponsiveContainer><LineChart data={data}><XAxis dataKey="label"/><YAxis allowDecimals={false}/><Tooltip/><Legend/><Line dataKey="total" name="Total events" stroke="#06b6d4"/><Line dataKey="completed" name="Completed" stroke="#10b981"/></LineChart></ResponsiveContainer></div></section>}

import type{CanonicalEvent}from'@shared/contracts'
export function ActivityFeed({events}:{events:CanonicalEvent[]}){return <section className="glass-card p-5"><h2 className="mb-4 font-semibold">Recent Activity</h2>{events.length?<div className="space-y-3">{events.map(e=><div key={e.id} className="rounded-lg p-3"><div className="flex justify-between gap-2"><p className="text-sm">{e.eventType}</p><span className="text-xs capitalize text-dvn-text-secondary">{e.status}</span></div><p className="text-xs text-dvn-text-secondary">{e.agentId??'System'} · {e.runtimeKind??e.entityType} · {new Date(e.occurredAt).toLocaleString()}</p></div>)}</div>:<p className="py-12 text-center text-sm text-dvn-text-secondary">No verified lifecycle activity is available.</p>}</section>}

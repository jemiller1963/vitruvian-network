import{describe,expect,it}from'vitest';import{previewActivity,previewAgents,previewMetrics,previewSystem}from'./preview';describe('preview fixture',()=>{it('contains the approved isolated roster and explicitly unavailable telemetry',()=>{expect(previewAgents.map(x=>x.id)).toEqual(['main','researcher','raph','designer','analyst','backend','frontend','qa','deployment']);expect(previewAgents.every(x=>x.runtimeStatus==='unknown')).toBe(true);expect(previewActivity.daily).toHaveLength(7);expect(previewActivity.byAgentHour).toHaveLength(1512);expect(previewActivity.status).toBe('unavailable');expect(previewMetrics.workingNow.value).toBeNull();expect(previewSystem.gateway.reason).toContain('Fixture preview')})})

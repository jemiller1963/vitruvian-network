import{describe,expect,it}from'vitest';import{previewActivity,previewAgents,previewSystem}from'./preview';describe('preview fixture',()=>{it('contains the approved isolated roster and explicit zero state',()=>{expect(previewAgents.map(x=>x.id)).toEqual(['main','researcher','raph','designer','analyst','backend','frontend','qa','deployment']);expect(previewAgents.every(x=>x.runtimeStatus==='unknown')).toBe(true);expect(previewActivity.daily).toHaveLength(7);expect(previewActivity.byAgentHour).toHaveLength(216);expect(previewSystem.gateway.reason).toContain('Fixture preview')})})
